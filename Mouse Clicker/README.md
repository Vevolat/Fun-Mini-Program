# 🖱️ 智能鼠标连点器

一个基于Python和Tkinter的现代化鼠标连点器应用程序，具有美观的界面和丰富的功能。

## 🌟 功能特点

- 支持左键、右键和双击左键模式
- 可自定义点击间隔时间（0.001秒到10秒，支持极速连点）
- 可设置点击次数（0为无限点击）
- 支持热键操作（默认F6开始，F7停止，可自定义）
- 美观的现代化图形用户界面
- 支持固定位置点击和当前鼠标位置点击
- 实时状态显示和进度跟踪
- 自动保存配置，无需每次重新设置

## 📦 安装依赖

```bash
pip install -r requirements.txt
```

## ▶️ 使用方法

1. 运行程序：
```bash
python main.py
```

2. 设置点击参数：
   - 调整点击间隔时间
   - 设置点击次数（0为无限点击）
   - 选择鼠标按键类型（左键、右键或双击）
   - 选择点击位置（当前鼠标位置或固定位置）
   - 设置固定位置（如果选择了固定位置模式）

3. 自定义热键：
   - 点击"设置"按钮进入热键设置
   - 点击开始或停止按钮旁的按键来更改热键

4. 开始使用：
   - 点击"开始"按钮或按设置的开始热键开始连点
   - 点击"停止"按钮或按设置的停止热键停止连点

## ⚠️ 注意事项

- 使用时请注意控制点击频率，避免对目标应用造成影响
- 如需紧急停止，可以移动鼠标到屏幕左上角（PyAutoGUI的安全措施）或按停止热键
- 本工具仅供学习和正当用途，请勿用于不当目的

## 🛠️ 依赖库

- tkinter (Python标准库)
- pyautogui>=0.9.54
- keyboard>=0.13.5
